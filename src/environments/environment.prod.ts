import { EnvVariable } from './environments.model';

export const environment: EnvVariable = {
  production: true,
  baseURL: 'https://api-dev.takenotetyping.com',
  env: 'prod',
  logState: false,
};
